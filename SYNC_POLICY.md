# 同步規則

- 同步來源是本目錄中的規格、統計清冊、`example/` 原始題庫與 `sample/` 已交付樣卷；既有樣卷的驗證紀錄不代表最新命題或視覺風格已獲核准。
- `樣張`、抽圖、稽核程式、Word 暫存轉檔、DOCX 渲染 PNG／PDF、分析接觸表與其他暫存檔不會上傳。
- 新批次同步已驗證的圖片成品、必要可編輯來源、數據及不含 prompt 的 metadata，題本引用與檔案須一致。既有 `figures.html` 與其 PNG 的對應屬歷史製程，不要求所有新圖片一律採 HTML／SVG。
- `work/questions.draft.md`、`work/questions.illustrated.md`、內部圖片處理 manifest 及其他包含 AI 圖片 prompt 的工作資料不納入預設同步；題本 `questions.md`、交付 Word、`questions.jsonl` 與資產 metadata 必須已清除 prompt、negative_prompt 及 IMAGE_REQUEST 區塊。每份同步的 Word 題目卷與解析卷須已通過 inline 圖片、`asset_id → anchor → 題號 → 段落順序`、替代文字及逐頁渲染檢查；只同步正式 DOCX，不同步渲染 PNG／PDF。此限制適用生成題本與資產，規格文件說明交換格式的範例可保留。B 生成失敗時先保留內部工作稿供續作，未完成回填的題本不得作成品同步。
- 每次執行工作完成後，才在同一次工作中檢查差異；有實質變更才提交並推送，無變更時保持安靜。不得建立每 15 分鐘或任何其他獨立週期的同步排程。
- 同步範圍限本次任務相關且已授權的成果。推送前除工作目錄差異外，也要檢查所有待推送提交；若夾帶先前未推送的題庫、樣卷或其他無關檔案，先隔離本次文件提交，不直接推送整條本機分支，也不強制覆寫遠端。本次 MD 修訂不代表新增上傳原始題庫的授權。
- 提交訊息使用繁體中文，說明修改目的與影響。
- 推送前檢查不得含密碼、存取權杖、個人認證資料或本機絕對路徑。
